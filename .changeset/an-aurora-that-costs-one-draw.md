---
'overwander': patch
---

The aurora is worked out into a field and drawn in one pass, the way a rainbow already was, instead of a blit per fold per band. Its colours and the way it hangs are unchanged.
