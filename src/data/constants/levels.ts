/**
 * The level every pokemon starts from
 */
export const MIN_LEVEL = 1;

/**
 * The highest level a pokemon can reach, however it grows — spawns
 * roll up to it and candies stop feeding at it
 */
export const MAX_LEVEL = 100;
