---
'overwander': patch
---

The sprite processor is its own page at /sprite-processor now, rather than a section of the admin dashboard, and asks nobody to sign in. It still only runs on a development build, which is the whole of what guards it.
